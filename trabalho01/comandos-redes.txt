git clone git clone https://github.com/coreemu/core.git

cd core/

./install.sh


sudo systemctl daemon-reload
sudo systemctl start core-daemon

------------------Se não der - tentar com $sudo core-daemon --------------------------------

core-gui
